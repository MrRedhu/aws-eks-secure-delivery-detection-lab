apiVersion: v1
kind: Service
metadata:
  name: secure-demo-api
  namespace: secure-demo
  labels:
    app.kubernetes.io/name: secure-demo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: secure-demo
  ports:
    - name: http
      port: 80
      targetPort: http
